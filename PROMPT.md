study the .specs/cli_migration.md

Your task is to complete all of the tasks on the list. Tasks with [x] are completed, don't work on those. work on the first task without a completion mark.

when you're done with completing a task, run the build "vp build" in the root, then run TYPE CHECKS ONLY. ensure that YOUR CHANGES ONLY don't have type-errros (this is because there may be a significant amount of existing type-errors, don't try to fix those)
